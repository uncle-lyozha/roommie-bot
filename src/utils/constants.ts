export enum taskStatus {
    new = "new",
    pending = "pending",
    snoozed = "snoozed",
    done = "done",
    failed = "failed"
}

export enum tgUserReplyOption {
    confirm = "confirm",
    done = "done",
    snooze = "snooze",
    help = "help"
}
