export enum taskStatus {
    new = "new",
    pending = "pending",
    snoozed = "snoozed",
    done = "done",
}

export enum tgUserReplyOption {
    confirm = "confirm",
    done = "done",
    snooze = "snooze",
}
